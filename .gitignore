# Misc
.DS_Store
._.DS_Store
**/.DS_Store
**/._.DS_Store
.codegraph

# Xcode — project is generated by XcodeGen (run: xcodegen generate)
*.xcodeproj
xcuserdata/
DerivedData/

# SwiftPM
.build/
.swiftpm/

# Per-developer signing override (see Config/Debug.xcconfig)
Local.xcconfig

# Rust
services/target/

# Per-developer service config (see services/.env.example)
services/.env

# Railway IaC evaluator dependencies (.railway/package.json is committed)
.railway/node_modules/

# Scratch: screenshots, recordings, and sample images used while building.
# Never committed — some of it is footage of real people.
tmp/
