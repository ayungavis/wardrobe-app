// Production environment values. API_BASE_URL and SENTRY_DSN are deliberately
// absent: the TestFlight workflow passes them to xcodebuild from GitHub Actions
// secrets, so they never live in the repository. A local release build supplies
// them the same way — see "Release builds" in the README.
SENTRY_ENVIRONMENT = production
CONSENT_AI_PROVIDER = Seedream (ByteDance)
